# dotnet-uptime/demo

Demonstrates and tests various components used by dotnet-uptime. Requires a single command-line argument.

* `demo list` - outputs all processes exposing a .NET diagnostics port

